# Animations

Place Lottie (`.json`) and Rive animation files here for MindSafe UI micro-interactions.

Suggested assets:
- `onboarding_*.json` — welcome flow illustrations
- `mood_success.json` — mood saved confirmation
- `breathing_guide.json` — optional breathing exercise overlay

Referenced in `pubspec.yaml` under `assets/animations/`.
