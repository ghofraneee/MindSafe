# Data

Place bundled JSON or seed data files here for offline-first features.

Suggested assets:
- `quotes.json` — inspirational quotes for home screen
- `helplines.json` — optional regional crisis line overrides
- `achievements.json` — achievement definitions (if moved from code)

Referenced in `pubspec.yaml` under `assets/data/`.
