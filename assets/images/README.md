# Images

Place static image assets here (PNG, WebP, SVG via flutter_svg if added).

Suggested assets:
- `logo.png` — MindSafe app icon / splash
- `avatar_placeholder.png` — default profile avatar
- `wellness_hero.webp` — home screen hero illustration

Referenced in `pubspec.yaml` under `assets/images/`.
